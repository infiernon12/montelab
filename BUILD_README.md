# 🔨 MonteLab Build Instructions

Инструкция по сборке защищенной версии MonteLab с компиляцией бизнес-логики.

---

## 📋 Стратегия сборки

### ✅ Что компилируется (Nuitka)
- **Core modules**: domain, poker logic
- **Utils**: HWID, license, screen capture
- **Services**: analysis service (НЕ ml_service!)
- **UI**: только widgets и dialogs (НЕ windows!)

### ⚠️ Что НЕ компилируется (остается как Python)
- **ml/detector.py** - нейросети YOLO и ResNet
- **ml/__init__.py** - импорт detector
- **services/ml_service.py** - использует detector
- **ui/ml_worker.py** - использует ml_service
- **ui/windows/*.py** - импортируют MLWorker и MLService
- **ui/dock_widgets.py** - может импортировать ml_worker
- **Ultralytics** и PyTorch - уже скомпилированы в C++

### 💡 Почему так?
- Нейросети уже оптимизированы (PyTorch C++ extensions)
- Ultralytics имеет сложные зависимости
- Быстрее сборка (10-20 мин вместо часов)
- Легче отладка ML кода
- ML фреймворки не стоит компилировать

### ⚠️ ВАЖНО: Почему UI windows не компилируются?

**Проблема цепочки импортов:**
```python
ui/windows/main_window.py (пытаемся скомпилировать)
  └── from ui.ml_worker import MLWorker
        └── from services.ml_service import MLService
              └── from ml.detector import TableCardDetector
                    └── from ultralytics import YOLO  # ❌ Проблема!
```

**Что происходит:**
1. Nuitka компилирует `main_window.py` → `main_window.pyd`
2. Скомпилированный файл содержит импорт `MLWorker`
3. `MLWorker` импортирует `ml_service`
4. `ml_service` импортирует `detector`
5. `detector` импортирует `ultralytics`

**Потенциальные проблемы:**
- Nuitka может попытаться анализировать всю цепочку
- PyInstaller может не увидеть транзитивные зависимости
- Возможны конфликты путей импорта в runtime
- Сложнее отладка при ошибках импорта

**Решение:**
- НЕ компилируем модули, которые импортируют ML
- Оставляем UI windows как обычный Python код
- Это безопасно и не влияет на производительность (UI не критичен для скорости)

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install nuitka pyinstaller
```

### 2. Запуск сборки

```bash
python build_secure.py
```

### 3. Результат

```
dist/MonteLab/
├── MonteLab.exe (или MonteLab)
├── models/
│   ├── board_player_detector_v4.pt
│   └── fine_tuned_resnet_cards_240EPOCH.pt
└── [другие файлы]
```

---

## 📁 Созданные файлы

### 1. **main_secure.py**
Точка входа для скомпилированной версии:
```python
from main_start import main
sys.exit(main())
```

### 2. **build_secure.py**
Скрипт сборки:
- Компиляция модулей через Nuitka
- Сборка через PyInstaller
- Автоматическая очистка

### 3. **MonteLab_secure.spec**
Конфигурация PyInstaller:
- Скрытые импорты
- Исключения
- Data files

---

## ⚙️ Детали сборки

### Модули для компиляции (Nuitka)

**Core Domain (4 модуля):**
```
core/domain/card.py
core/domain/game_state.py
core/domain/detection.py
```

**Core Poker (5 модулей):**
```
core/poker/hand_evaluator.py
core/poker/outs_calculator.py
core/poker/board_analyzer.py
core/poker/equity_calculator.py
core/poker/monte_carlo_backend.py
```

**Utils (4 модуля):**
```
utils/hwid_generator.py
utils/license_client.py
utils/screen_capture.py
utils/global_hotkeys.py
```

**Services (2 модуля):**
```
services/analysis_service.py
services/improved_abc_recommendations.py
```

**UI (5 модулей):**
```
ui/hwid_dialog.py
ui/styles.py
ui/ui_config.py
ui/widgets/card_input.py
ui/widgets/selection_overlay.py
```

**Итого: ~20 модулей**

### Модули исключенные из компиляции

```
# ML modules
ml/detector.py          # Нейросети
ml/__init__.py          # Импорт detector
services/ml_service.py  # Использует detector
ui/ml_worker.py         # Использует ml_service

# UI that imports ML (ВАЖНО!)
ui/dock_widgets.py      # Может импортировать ml_worker
ui/windows/main_window.py          # Импортирует MLWorker, MLService
ui/windows/adaptive_main_window.py # Импортирует MLWorker, MLService

# Entry points
main_start.py           # Entry point
main_secure.py          # Entry point
```

**Почему UI windows не компилируются:**
- Они импортируют `MLWorker` и `MLService`
- Это создает цепочку зависимостей: UI → ml_worker → ml_service → detector → ultralytics
- Nuitka с `--nofollow-imports` не скомпилирует зависимости, но может быть конфликт
- Безопаснее оставить их как Python

---

## 🕐 Время сборки

| Этап | Время |
|------|-------|
| Nuitka compilation | 10-20 минут |
| PyInstaller packaging | 5-10 минут |
| **Итого** | **15-30 минут** |

*(Зависит от CPU и количества ядер)*

---

## 📦 Размер сборки

| Компонент | Размер |
|-----------|--------|
| Executable | ~50-100 MB |
| PyTorch libs | ~500-800 MB |
| Models (.pt) | ~100-200 MB |
| **Итого** | **~700-1100 MB** |

---

## 🐛 Troubleshooting

### Ошибка: "Nuitka not found"
```bash
pip install nuitka
```

### Ошибка: "PyInstaller not found"
```bash
pip install pyinstaller
```

### Ошибка: "Module compilation failed"
- Проверьте, что модуль существует
- Проверьте синтаксис Python в модуле
- Пропустите модуль (сборка продолжится)

### Ошибка: "PyInstaller failed"
- Проверьте логи в `build/`
- Убедитесь, что все зависимости установлены
- Попробуйте без флага `--clean`

### Executable не запускается
- Проверьте наличие `models/*.pt` файлов
- Проверьте права на запуск
- Запустите с консолью: измените `console=False` на `console=True` в .spec

---

## 🔧 Ручная сборка (опционально)

### Только Nuitka (без PyInstaller)

```bash
# Компиляция одного модуля
python -m nuitka --module core/domain/card.py

# Компиляция всех core модулей
for file in core/**/*.py; do
    python -m nuitka --module "$file"
done
```

### Только PyInstaller (без Nuitka)

```bash
pyinstaller --clean --noconfirm MonteLab_secure.spec
```

---

## 📊 Сравнение с старой сборкой

| Параметр | Старая (build_fixed_v2.py) | Новая (build_secure.py) |
|----------|---------------------------|------------------------|
| Компилирует ML | ❌ Да | ✅ Нет |
| Время сборки | 30-60 мин | 15-30 мин |
| Размер | ~1.2-1.5 GB | ~700-1100 MB |
| Отладка ML | Сложно | Легко |
| Производительность | Одинаковая | Одинаковая |

---

## ✅ Проверка сборки

После успешной сборки проверьте:

1. **Executable создан:**
   ```bash
   ls dist/MonteLab/MonteLab.exe  # Windows
   ls dist/MonteLab/MonteLab      # Linux/Mac
   ```

2. **Модели присутствуют:**
   ```bash
   ls dist/MonteLab/models/*.pt
   ```

3. **ML модули как Python:**
   ```bash
   ls dist/MonteLab/ml/detector.py  # Должен существовать
   ```

4. **Компилированные модули:**
   ```bash
   ls dist/MonteLab/**/*.pyd  # Windows
   ls dist/MonteLab/**/*.so   # Linux/Mac
   ```

---

## 🎯 Следующие шаги

После успешной сборки:

1. **Тестирование:**
   ```bash
   cd dist/MonteLab
   ./MonteLab.exe  # или ./MonteLab
   ```

2. **Проверка GPU:**
   - Убедитесь, что логи показывают GPU detection
   - Проверьте FP16 и torch.compile

3. **Проверка производительности:**
   - Запустите детекцию карт
   - Проверьте timing логи
   - Убедитесь в работе кеша

4. **Создание дистрибутива:**
   ```bash
   # Упаковка в архив
   cd dist
   zip -r MonteLab.zip MonteLab/
   # или
   tar -czf MonteLab.tar.gz MonteLab/
   ```

---

## 📝 Логи сборки

Логи сохраняются в:
- `build/` - Nuitka logs
- `*.log` - PyInstaller logs

При ошибках проверяйте эти файлы.

---

## 💡 Советы

1. **Используйте виртуальное окружение:**
   ```bash
   python -m venv build_env
   source build_env/bin/activate  # Linux/Mac
   build_env\Scripts\activate     # Windows
   pip install -r requirements.txt
   python build_secure.py
   ```

2. **Параллельная компиляция (Nuitka):**
   - Nuitka использует все ядра CPU автоматически
   - Для ускорения: закройте другие программы

3. **Чистая сборка:**
   ```bash
   # Удалить старые артефакты
   rm -rf build/ dist/ *.spec python_backup/
   # Удалить .pyd/.so файлы
   find . -name "*.pyd" -delete
   find . -name "*.so" -delete
   ```

---

## 🔒 Безопасность

### Что защищено
- Бизнес-логика скомпилирована в бинарный код
- HWID generation
- License client
- Алгоритмы анализа покера

### Что НЕ защищено
- ML модели (.pt файлы) - обычные PyTorch модели
- ML код (detector.py) - обычный Python
- Конфигурация

### Рекомендации
- Не распространяйте исходники
- Используйте обфускацию для .py файлов (опционально)
- Защитите .pt модели (шифрование, опционально)

---

## 📞 Поддержка

При проблемах:
1. Проверьте секцию Troubleshooting
2. Проверьте логи сборки
3. Убедитесь, что все зависимости установлены
4. Попробуйте чистую сборку

---

**Версия:** 1.0
**Дата:** 12.11.2025
**Автор:** Claude (Anthropic)
